require 'spec_helper'

describe Penelope::Worker do
  it 'has a version number' do
    expect(Penelope::Worker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
